public class Ejercicio4 {
    public static void main(String[] args) {
        //Muestra los números del 1 al 100 (ambos incluidos). Usa un bucle while.
        //Mismo ejercicio pero con el bucle for

        int ejemploNumero = 0;

        for (int index = 100; ejemploNumero <= index; ejemploNumero++) {
            System.out.println(ejemploNumero);
        }

    }
}
